# MCP Smoke Evidence - 2026-06-13

Command:

```bash
uv run heddle mcp-smoke --repo . --json
```

Result:

- Schema: `heddle.mcp_smoke.v1`
- Transport: `stdio`
- Overall status: `ok: true`
- `initialize` returned protocol version `2025-03-26`, server name `heddle`,
  server version `0.1.0`, and tool capability metadata.
- `tools/list` returned the five current compatibility tools:
  `blast_radius`, `capture_snapshot`, `changed`, `reverify`, and `timeline`.
- `changed` returned a structured `heddle.draft.changed.v1` payload.
- A bad revision range returned JSON-RPC `-32602` with structured
  `heddle.error.v1` data, `error_code: invalid_rev_range`, rejected field
  `rev_range`, and `retryability: retry_with_changes`.
- The server answered a second `tools/list` after the tool error.

Interpretation:

This satisfies the current MCP survivability check. It does not freeze the MCP
contract: namespaced aliases, specific output schemas, list filters/sort,
pagination, and resource contracts remain the P1 MCP contract refactor.
